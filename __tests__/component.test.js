/*
import React from 'react';

import renderer from 'react-test-renderer';
import CharacterItemComponent from '../src/components/CharacterItemComponent';
import {Characher} from '../src/models/CharacterModel';

describe('Test component render', () => {
  let character = new Characher({
    name: 'taha',
    occupation: [],
    appearance: [],
    better_call_saul_appearance: [],
  });

  const tree = renderer
    .create(<CharacterItemComponent characters={character} />)
    .toJSON();
  expect(tree).toMatchSnapshot();

  // it('test ', () => {
  //   const mockEditTodoItemFn = jest.fn();
  //   const {component} = renderer(
  //     <CharacterItemComponent characters={mockEditTodoItemFn} />,
  //   );
  // });
});
*/
